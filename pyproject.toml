[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-desk-lab"
version = "0.1.0"
description = "Offline, deterministic historical simulation lab. No live execution."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["trading_lab*"]
