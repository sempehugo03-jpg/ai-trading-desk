[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-desk-lab"
version = "2.0.0"
description = "Market-first discovery engine with progressive evidence gates and protected OOS. No live execution."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["trading_lab*"]
