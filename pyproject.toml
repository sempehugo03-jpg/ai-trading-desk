[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-desk-lab"
version = "0.2.0"
description = "Deterministic multi-asset historical research lab with real BID/ASK data adapters. No live execution."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["trading_lab*"]
