[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="ai-trading-desk-lab"
version="3.0.0"
description="Open symbolic cross-asset discovery with double protected OOS. No live execution."
requires-python=">=3.11"
dependencies=[]
[tool.setuptools.packages.find]
include=["trading_lab*"]
