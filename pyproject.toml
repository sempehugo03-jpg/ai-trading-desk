[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-desk-lab"
version = "0.3.0"
description = "Deterministic multi-asset research lab with continuous staged research and one-shot blind-box evaluation. No live execution."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["trading_lab*"]
