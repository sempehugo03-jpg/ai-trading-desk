[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-desk-lab"
version = "0.5.0"
description = "Parallel multi-agent research desk with deterministic quant gates, blind-box validation and no live execution."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["trading_lab*"]
